using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using System;

public class LevelManager : MonoBehaviour {
     
    // Level currentLevel;
    // Stage stage1, stage2, stage3;
     
    void Start() {
        //load saved data and player prefs
        // currentLevel = new Level1(stage1); // For now I am always loading level 1, I want to load the last one visited (the saved level)
    }
     
}